-- Generated by aether-data-schema from schema/logical/*.toml.
-- Do not edit generated files directly; edit logical schema or explicit overrides instead.

CREATE TABLE IF NOT EXISTS risk_control_logs (
    id TEXT PRIMARY KEY NOT NULL,
    trace_id TEXT NOT NULL,
    request_id TEXT,
    user_id TEXT,
    username TEXT,
    user_email TEXT,
    api_key_id TEXT,
    api_key_name TEXT,
    route_family TEXT,
    route_kind TEXT,
    api_format TEXT,
    endpoint TEXT NOT NULL,
    model TEXT,
    mode TEXT NOT NULL,
    action TEXT NOT NULL,
    decision_source TEXT NOT NULL,
    flagged INTEGER NOT NULL DEFAULT 0,
    highest_category TEXT,
    highest_score REAL NOT NULL DEFAULT 0,
    category_scores TEXT,
    thresholds TEXT,
    matched_keywords TEXT,
    input_hash TEXT,
    excerpt TEXT,
    latency_ms INTEGER,
    queue_delay_ms INTEGER,
    violation_count INTEGER NOT NULL DEFAULT 0,
    auto_action TEXT,
    notification_sent INTEGER NOT NULL DEFAULT 0,
    notification_attempts INTEGER NOT NULL DEFAULT 0,
    notification_last_error TEXT,
    notification_last_attempt_at INTEGER,
    error_message TEXT,
    created_at INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_created_at ON risk_control_logs (created_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_flagged_created_at ON risk_control_logs (flagged, created_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_user_created_at ON risk_control_logs (user_id, created_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_api_key_created_at ON risk_control_logs (api_key_id, created_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_endpoint_created_at ON risk_control_logs (endpoint, created_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_input_hash ON risk_control_logs (input_hash);

CREATE TABLE IF NOT EXISTS risk_control_flagged_hashes (
    input_hash TEXT PRIMARY KEY NOT NULL,
    source_log_id TEXT,
    reason TEXT,
    highest_category TEXT,
    highest_score REAL NOT NULL DEFAULT 0,
    excerpt TEXT,
    first_seen_at INTEGER NOT NULL,
    last_seen_at INTEGER NOT NULL,
    hit_count INTEGER NOT NULL DEFAULT 1
);
CREATE INDEX IF NOT EXISTS idx_risk_control_hashes_last_seen_at ON risk_control_flagged_hashes (last_seen_at);

CREATE TABLE IF NOT EXISTS risk_control_notification_outbox (
    id TEXT PRIMARY KEY NOT NULL,
    log_id TEXT NOT NULL,
    item_key TEXT NOT NULL,
    title TEXT NOT NULL,
    markdown_body TEXT NOT NULL,
    text_body TEXT NOT NULL,
    variables_json TEXT NOT NULL,
    status TEXT NOT NULL,
    attempt_count INTEGER NOT NULL DEFAULT 0,
    max_attempts INTEGER NOT NULL DEFAULT 10,
    next_attempt_at INTEGER,
    lease_until INTEGER,
    last_error TEXT,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    sent_at INTEGER,
    UNIQUE (log_id, item_key)
);
CREATE INDEX IF NOT EXISTS idx_risk_control_notification_outbox_due ON risk_control_notification_outbox (status, next_attempt_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_notification_outbox_lease ON risk_control_notification_outbox (status, lease_until);
CREATE INDEX IF NOT EXISTS idx_risk_control_notification_outbox_updated ON risk_control_notification_outbox (updated_at);

