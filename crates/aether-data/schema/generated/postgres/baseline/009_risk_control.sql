-- Generated by aether-data-schema from schema/logical/*.toml.
-- Do not edit generated files directly; edit logical schema or explicit overrides instead.

CREATE TABLE IF NOT EXISTS public.risk_control_logs (
    id character varying(36) NOT NULL,
    trace_id character varying(100) NOT NULL,
    request_id character varying(100),
    user_id character varying(36),
    username character varying(100),
    user_email character varying(255),
    api_key_id character varying(36),
    api_key_name character varying(255),
    route_family character varying(80),
    route_kind character varying(80),
    api_format character varying(80),
    endpoint character varying(255) NOT NULL,
    model character varying(255),
    mode character varying(32) NOT NULL,
    action character varying(32) NOT NULL,
    decision_source character varying(32) NOT NULL,
    flagged boolean DEFAULT false NOT NULL,
    highest_category character varying(100),
    highest_score double precision DEFAULT 0 NOT NULL,
    category_scores jsonb,
    thresholds jsonb,
    matched_keywords jsonb,
    input_hash character varying(64),
    excerpt text,
    latency_ms bigint,
    queue_delay_ms bigint,
    violation_count bigint DEFAULT 0 NOT NULL,
    auto_action character varying(64),
    notification_sent boolean DEFAULT false NOT NULL,
    notification_attempts bigint DEFAULT 0 NOT NULL,
    notification_last_error text,
    notification_last_attempt_at bigint,
    error_message text,
    created_at bigint NOT NULL
);

ALTER TABLE ONLY public.risk_control_logs ADD CONSTRAINT risk_control_logs_pkey PRIMARY KEY (id);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_created_at ON public.risk_control_logs USING btree (created_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_flagged_created_at ON public.risk_control_logs USING btree (flagged, created_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_user_created_at ON public.risk_control_logs USING btree (user_id, created_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_api_key_created_at ON public.risk_control_logs USING btree (api_key_id, created_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_endpoint_created_at ON public.risk_control_logs USING btree (endpoint, created_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_logs_input_hash ON public.risk_control_logs USING btree (input_hash);

CREATE TABLE IF NOT EXISTS public.risk_control_flagged_hashes (
    input_hash character varying(64) NOT NULL,
    source_log_id character varying(36),
    reason character varying(255),
    highest_category character varying(100),
    highest_score double precision DEFAULT 0 NOT NULL,
    excerpt text,
    first_seen_at bigint NOT NULL,
    last_seen_at bigint NOT NULL,
    hit_count bigint DEFAULT 1 NOT NULL
);

ALTER TABLE ONLY public.risk_control_flagged_hashes ADD CONSTRAINT risk_control_flagged_hashes_pkey PRIMARY KEY (input_hash);
CREATE INDEX IF NOT EXISTS idx_risk_control_hashes_last_seen_at ON public.risk_control_flagged_hashes USING btree (last_seen_at);

CREATE TABLE IF NOT EXISTS public.risk_control_notification_outbox (
    id character varying(36) NOT NULL,
    log_id character varying(36) NOT NULL,
    item_key character varying(120) NOT NULL,
    title character varying(255) NOT NULL,
    markdown_body text NOT NULL,
    text_body text NOT NULL,
    variables_json jsonb NOT NULL,
    status character varying(32) NOT NULL,
    attempt_count bigint DEFAULT 0 NOT NULL,
    max_attempts bigint DEFAULT 10 NOT NULL,
    next_attempt_at bigint,
    lease_until bigint,
    last_error text,
    created_at bigint NOT NULL,
    updated_at bigint NOT NULL,
    sent_at bigint
);

ALTER TABLE ONLY public.risk_control_notification_outbox ADD CONSTRAINT risk_control_notification_outbox_pkey PRIMARY KEY (id);
ALTER TABLE ONLY public.risk_control_notification_outbox ADD CONSTRAINT uq_risk_control_notification_outbox_log_item UNIQUE (log_id, item_key);
CREATE INDEX IF NOT EXISTS idx_risk_control_notification_outbox_due ON public.risk_control_notification_outbox USING btree (status, next_attempt_at);
CREATE INDEX IF NOT EXISTS idx_risk_control_notification_outbox_lease ON public.risk_control_notification_outbox USING btree (status, lease_until);
CREATE INDEX IF NOT EXISTS idx_risk_control_notification_outbox_updated ON public.risk_control_notification_outbox USING btree (updated_at);
